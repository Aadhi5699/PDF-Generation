  $targetDate = date('Y-m-01', strtotime('first day of previous month'));
  $reportMonth = str_pad(date('m', strtotime($targetDate)), 2, '0', STR_PAD_LEFT);
  $reportYear = date('Y', strtotime($targetDate))
